<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.gestion_medicale.controllers.DoctorController"
      spacing="16" styleClass="view-container">
    <padding><Insets top="20" right="20" bottom="20" left="20"/></padding>

    <Label text="👨‍⚕️ Gestion des Médecins" styleClass="view-title"/>

    <SplitPane dividerPositions="0.6" VBox.vgrow="ALWAYS">

        <!-- TABLE -->
        <VBox spacing="8">
            <TableView fx:id="tableDoctors" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                    <TableColumn fx:id="colNom" text="Nom" prefWidth="180"/>
                    <TableColumn fx:id="colSpecialite" text="Spécialité" prefWidth="150"/>
                </columns>
                <columnResizePolicy><TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/></columnResizePolicy>
            </TableView>
        </VBox>

        <!-- FORM -->
        <VBox spacing="12" styleClass="form-panel">
            <padding><Insets top="10" right="10" bottom="10" left="10"/></padding>

            <Label text="Informations Médecin" styleClass="form-section-title"/>

            <Label text="Nom *" styleClass="form-label"/>
            <TextField fx:id="txtNom" promptText="Nom du médecin" styleClass="form-field"/>

            <Label text="Spécialité" styleClass="form-label"/>
            <ComboBox fx:id="cmbSpecialite" promptText="Sélectionner une spécialité"
                      maxWidth="Infinity" styleClass="form-field"/>

            <Label fx:id="lblMessage" text="" wrapText="true"/>

            <HBox spacing="8">
                <Button text="✚ Ajouter" onAction="#handleAjouter" styleClass="btn-primary" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
                <Button text="✎ Modifier" onAction="#handleModifier" styleClass="btn-secondary" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
            </HBox>
            <HBox spacing="8">
                <Button text="✕ Supprimer" onAction="#handleSupprimer" styleClass="btn-danger" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
                <Button text="↺ Effacer" onAction="#handleEffacer" styleClass="btn-outline" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
            </HBox>
        </VBox>
    </SplitPane>
</VBox>